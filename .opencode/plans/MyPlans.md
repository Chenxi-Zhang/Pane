## Plan tab
* 这里是项目级临时文件？有什么想法可以随时写下来？

## Explorer
* Git ignore 要不要变灰

## Terminal
* 从其他session切换回来时，Opencode TUI没有全屏刷新？刷新按钮并不能解决问题？如果我修改pane窗口的size，能恢复，是opencode内部的绘制的能力
* 重获焦点时，主线程严重卡顿一段时间

## Super Client
* 它能不能和terminal里调用的TUI交互？或者至少和CLI交互。它来理解发生了什么？
* 它能直接对话？用openclaw？
* 它能和我一起plan需求细节，一个需求一个session，对完了之后就可以自己控制session，依次实现。
